# Agent boundary

Business-intelligence agents should expose typed inputs and outputs, use least-privilege database access, and emit auditable execution events. Keep agent orchestration separate from HTTP handlers.

